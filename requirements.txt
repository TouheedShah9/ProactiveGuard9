streamlit>=1.28.0
plotly>=5.0.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
xgboost>=2.0.0
# shap>=0.51.0 would fix the xgboost 3.x base_score parsing bug
# directly, but that version requires Python>=3.11. On Python 3.10,
# pip resolves shap 0.49.1, which still hits that bug -- so instead
# of relying on a version pin, v2_model.py and v2_dashboard.py both
# carry a code-level compatibility patch that works around it
# regardless of which shap version 3.10 users end up with.
shap>=0.42.0
scikit-learn>=1.2.0
imbalanced-learn>=0.11.0
optuna>=3.0.0
groq>=1.0.0
python-dotenv>=1.0.0
reportlab>=4.0.0
ephem>=4.1.0
